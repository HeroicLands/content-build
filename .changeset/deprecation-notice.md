---
---

README-only: the deprecation notice pointing at `@heroiclands/package-build`.

Deliberately empty — this package is merged and archived, and will not publish
again, so there is no version to bump.
